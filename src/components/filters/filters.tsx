import React from "react";
// 절대경로는 src 기준으로 '@/~' 경로로 사용하면 됩니다!
import Toggle from "@/components/common/toggle";

const Filters = () => {
  return (
    <>
      <Toggle />
    </>
  );
};

export default Filters;
